# Flash Dispatch - Quick Start Guide

## 🚀 One-Command Setup

Run this command to set up everything:

```bash
cd /workspaces/Flash-Dispatch-Transit
chmod +x create_setup.sh
./create_setup.sh
cd flash_dispatch